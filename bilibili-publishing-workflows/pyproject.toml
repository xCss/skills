[project]
name = "bilibili-publishing-workflows"
version = "2.0.0"
description = "Stable CLI for Bilibili poster publishing workflows"
requires-python = ">=3.11"
dependencies = [
  "pillow>=10.0.0",
  "playwright>=1.40.0",
  "qrcode>=7.4.0",
  "requests>=2.31.0",
]

[tool.uv]
package = false
